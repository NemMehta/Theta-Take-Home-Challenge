# task (taskbundle)

A Python CLI that packages SWE-bench-style coding tasks into Docker containers and runs/scores LLM solutions against them.

**WIP** — Phase 1 scaffold (command stubs + SQLite layer). See `CLAUDE.md` for the full design.
